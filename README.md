# 🕊️ Ren - Безопасный мессенджер с E2EE шифрованием

<div align="center">

![Ren Logo](https://img.shields.io/badge/Ren-Messenger-blue?style=for-the-badge&logo=messaging)
![License](https://img.shields.io/badge/license-Apache%202.0-green?style=for-the-badge)
![Rust](https://img.shields.io/badge/rust-1.75+-orange?style=for-the-badge&logo=rust)
![Flutter](https://img.shields.io/badge/flutter-3.8+-blue?style=for-the-badge&logo=flutter)
![React](https://img.shields.io/badge/react-19+-cyan?style=for-the-badge&logo=react)

**Современный кроссплатформенный мессенджер с сквозным шифрованием (End-to-End Encryption)**

[🚀 Начать](#-быстрый-запуск) • [📖 Документация](#-документация) • [🔧 API](#-api-спецификация) • [🛡️ Безопасность](#-безопасность)

</div>

## ✨ Особенности

### 🔒 **Безопасность на первом месте**
- **Сквозное шифрование (E2EE)** - все сообщения шифруются на устройстве отправителя и расшифровываются только на устройстве получателя
- **Асимметричная криптография** - у каждого пользователя есть пара публичный/приватный ключ
- **Система конвертов** - каждое сообщение шифруется отдельным ключом для каждого получателя
- **X25519 Diffie-Hellman** для обмена ключами
- **ChaCha20-Poly1305** для шифрования сообщений
- **PBKDF2** для хеширования паролей

### 🌐 **Кроссплатформенность**
- **Flutter** - мобильные приложения (iOS & Android)
- **React + TypeScript** - веб-приложение
- **Rust SDK** - ядро шифрования для всех платформ
- **WASM** поддержка для браузера

### 💬 **Современный функционал**
- Приватные и групповые чаты
- Обмен файлами с шифрованием
- Индикаторы набора текста
- Онлайн-статус пользователей
- WebSocket соединение для реального времени
- Управление аватарами

### ⚡ **Производительность**
- **Axum** - высокопроизводительный веб-сервер
- **PostgreSQL** - надёжная база данных
- **Асинхронная архитектура** на Tokio
- **Оптимизированный Rust SDK** с LTO

---

## 🏗️ Архитектура

```
Ren/
├── Ren-SDK/          # 🦀 Rust ядро шифрования
├── backend/          # 🚀 Axum сервер + PostgreSQL
├── apps/
│   ├── flutter/      # 📱 Flutter мобильные приложения
│   └── ios/          # 🍎 iOS нативное приложение
└── frontend/         # 🌐 React веб-приложение
```

### 📦 Технологический стек

| Компонент | Технология | Описание |
|-----------|------------|----------|
| **SDK** | Rust + WASM | Кроссплатформенное ядро E2EE |
| **Бэкенд** | Axum + PostgreSQL | REST API + WebSocket сервер |
| **Мобильные** | Flutter | iOS & Android приложения |
| **Веб** | React + TypeScript + Tailwind | Современный веб-интерфейс |
| **База данных** | PostgreSQL | Хранение пользователей, чатов, сообщений |
| **Шифрование** | X25519 + ChaCha20-Poly1305 | Криптографическая основа |

---

## 🚀 Быстрый запуск

### 📋 Требования
- Rust 1.75+
- Flutter 3.8+
- Node.js 18+
- PostgreSQL 14+
- Docker (опционально)

### 🛠️ Установка

1. **Клонирование репозитория**
```bash
git clone https://github.com/taiidzy/ren.git
cd ren
```

2. **Настройка бэкенда**
```bash
cd backend
cp .env.example .env
# Настройте DATABASE_URL и другие переменные
cargo run
```

3. **Сборка SDK**
```bash
cd ../Ren-SDK
cargo build --release
```

4. **Запуск Flutter приложения**
```bash
cd ../apps/flutter
flutter pub get
flutter run
```

### 🧰 Скрипты запуска и сборки (рекомендуется)

В репозитории есть набор скриптов в `./scripts/`, которые:
- не зависят от того, из какой директории вы их запускаете (автоматически находят корень репо),
- умеют опционально собирать `Ren-SDK` перед запуском/сборкой,
- дают единый интерфейс для `ios/macos/windows/linux`.

Важно:
- **iOS и macOS можно запускать/собирать только на macOS**.
- **Windows и Linux обычно требуют соответствующую ОС** (на macOS скрипт предупредит, но Flutter чаще всего не соберёт/не запустит).

#### `./scripts/run.sh` — запуск приложения

Синтаксис:
```bash
./scripts/run.sh <ios|macos|windows|linux> [options] [-- <extra flutter args>]
```

Параметры:
- `--sdk`
  - Собрать `Ren-SDK` перед запуском.
  - Вызовет `./Ren-SDK/build.sh <platform>`.
  - Для `ios` дополнительно скопирует `Ren-SDK/target/RenSDK.xcframework` в `apps/flutter/ios/RenSDK.xcframework`.
- `--debug`
  - Запуск в debug режиме.
  - **Режим по умолчанию**.
- `--release`
  - Запуск в release режиме (`flutter run --release`).
- `--device <id>`
  - Пробросит `-d <id>` в `flutter run`.
  - Полезно, если нужно выбрать конкретный симулятор/устройство.
  - Если не указано:
    - для `macos/windows/linux` будет использовано `-d <platform>`,
    - для `ios` `-d` не задаётся (Flutter сам предложит/выберет устройство).
- `-- <extra flutter args>`
  - Всё, что идёт после `--`, будет передано в `flutter run` как есть.

Примеры:
```bash
./scripts/run.sh ios --sdk
./scripts/run.sh ios --device "00008110-0012345678901234"
./scripts/run.sh macos
./scripts/run.sh linux --release
./scripts/run.sh windows -- --verbose
```

#### `./scripts/build.sh` — сборка приложения

Синтаксис:
```bash
./scripts/build.sh <ios|macos|windows|linux> [options] [-- <extra flutter args>]
```

Параметры:
- `--sdk`
  - Собрать `Ren-SDK` перед сборкой.
  - Для `ios` дополнительно скопирует `Ren-SDK/target/RenSDK.xcframework` в `apps/flutter/ios/RenSDK.xcframework`.
- `--release`
  - Release сборка.
  - **Режим по умолчанию**.
- `--debug`
  - Debug сборка.
- `--no-codesign` (только iOS)
  - Добавит `--no-codesign` к `flutter build ios`.
  - Удобно для сборки без настроенных signing-профилей.
- `--ipa` (только iOS)
  - Вместо `flutter build ios` вызовет `flutter build ipa`.
  - Артефакт будет в `apps/flutter/build/ios/ipa`.
- `--output <dir>`
  - Скопирует получившийся артефакт(ы) в указанную директорию.
  - Пример: `--output dist/`.
- `-- <extra flutter args>`
  - Всё, что идёт после `--`, будет передано в `flutter build ...` как есть.

Типовые команды:
```bash
./scripts/build.sh ios --sdk --no-codesign
./scripts/build.sh ios --ipa
./scripts/build.sh macos --release --output dist/
./scripts/build.sh linux --debug
./scripts/build.sh windows --release
```

Пути артефактов (по умолчанию, без `--output`):
- **iOS**:
  - `flutter build ios`: `apps/flutter/build/ios/iphoneos`
  - `flutter build ipa`: `apps/flutter/build/ios/ipa`
- **macOS**: `apps/flutter/build/macos/Build/Products/Release` или `.../Debug`
- **Windows**: `apps/flutter/build/windows/x64/runner/Release` или `.../Debug`
- **Linux**: `apps/flutter/build/linux/x64/release/bundle` или `.../debug/bundle`

#### Алиасы

Для удобства есть короткие алиасы, которые просто прокидывают аргументы в основные скрипты:
- Запуск:
  - `./scripts/run-ios.sh ...`
  - `./scripts/run-macos.sh ...`
  - `./scripts/run-windows.sh ...`
  - `./scripts/run-linux.sh ...`
- Сборка:
  - `./scripts/build-ios.sh ...`
  - `./scripts/build-macos.sh ...`
  - `./scripts/build-windows.sh ...`
  - `./scripts/build-linux.sh ...`

5. **Запуск веб-приложения**
```bash
cd ../frontend
npm install
npm run dev
```

### 🐳 Docker (альтернативный способ)
```bash
docker-compose up -d
```

---

## 📖 Документация

### 🔐 Архитектура шифрования

Ren использует асимметричную криптографию с системой конвертов:

1. **Пользовательские ключи** - каждый пользователь генерирует X25519 ключевую пару
2. **Публичные ключи** - хранятся на сервере для шифрования сообщений
3. **Приватные ключи** - хранятся на сервере в зашифрованном виде (мастер-ключ от пароля + соль)
4. **Система конвертов**:
   - Для каждого сообщения генерируется случайный симметричный ключ
   - Сообщение шифруется этим ключом (ChaCha20-Poly1305)
   - Ключ шифруется публичным ключом каждого получателя
   - Создаются отдельные "конверты" для каждого участника чата
5. **Расшифровка** - получатель расшифровывает свой приватный ключ мастер-ключом, затем использует его для расшифровки конверта

### 🔄 Поток сообщений

```
Отправитель                    Сервер                     Получатель
    |                           |                           |
    |-- Запрос PK получателя -->|                           |
    |-- Генерация msg_key ----->|                           |
    |-- Шифрование msg -------->|                           |
    |-- Создание конвертов ---->|                           |
    |-- Сообщение + конверты -->|---- Хранение ------------->|
    |                           |---- Рассылка ------------->|
    |                           |                           |-- Расшифровка конверта
    |                           |                           |-- Расшифровка сообщения
```

### 📱 Клиентские приложения

#### Flutter
- Кроссплатформенность (iOS/Android)
- Интеграция с Ren-SDK через FFI
- Безопасное хранение ключей
- Material Design интерфейс

#### React Web
- TypeScript для типобезопасности
- WASM модуль Ren-SDK
- Real-time обновления через WebSocket
- Адаптивный дизайн с Tailwind CSS

---

## 🔧 API Спецификация

### 🌐 Базовый URL
```
https://api.ren-messenger.com
```

### 🔑 Аутентификация
Все эндпоинты (кроме `/auth/*`) требуют JWT токен:
```
Authorization: Bearer <jwt>
```

### 📝 Основные эндпоинты

#### 👤 Пользователи
```http
POST   /auth/register     # Регистрация с E2EE ключами
POST   /auth/login        # Вход
GET    /users/me          # Профиль текущего пользователя
PATCH  /users/username    # Смена имени
PATCH  /users/avatar      # Загрузка аватара
DELETE /users/me          # Удаление аккаунта
```

#### 💬 Чаты
```http
POST   /chats             # Создание чата
GET    /chats             # Список чатов
GET    /chats/{id}/messages # Сообщения чата
DELETE /chats/{id}        # Удаление/выход из чата
```

#### 🔐 Криптография
```http
GET    /users/{id}/public-key # Публичный ключ для E2EE
```

### 🌐 WebSocket
Подключение: `GET /ws` с JWT токеном

#### События
```json
{
  "type": "init",
  "contacts": [1, 2, 3]
}

{
  "type": "join_chat",
  "chat_id": 123
}

{
  "type": "send_message",
  "chat_id": 123,
  "message": "base64_encrypted",
  "envelopes": {
    "2": {
      "key": "base64_wrapped_key",
      "ephem_pub_key": "base64_ephemeral_key",
      "iv": "base64_iv"
    }
  }
}
```

---

## 🛡️ Безопасность

### 🔒 Криптографические примитивы
- **Асимметричная криптография**: X25519 Elliptic Curve Diffie-Hellman
- **Симметричное шифрование**: ChaCha20-Poly1305 AEAD
- **Хеширование паролей**: Argon2id
- **Генерация случайных чисел**: getrandom() с системным источником
- **Система конвертов**: PKI-like подход для мультипользовательских чатов

### 🛡️ Меры защиты
- **Асимметричное шифрование** - только получатель может расшифровать свои конверты
- **Шифрование приватных ключей** - приватные ключи хранятся на сервере в зашифрованном виде
- **Защита от MITM** - проверка публичных ключей при регистрации
- **Мастер-ключ из пароля** - PBKDF2 с солью для шифрования приватных ключей
- **Zero-Knowledge** - сервер не имеет доступа к расшифрованным сообщениям
- **Отдельные конверты** - каждый участник получает свой зашифрованный ключ

### ⚠️ Важные замечания
- Сервер хранит только зашифрованные данные и конверты
- Приватные ключи хранятся на сервере в зашифрованном виде (мастер-ключ от пароля)
- Каждое сообщение использует уникальный симметричный ключ
- Конверты обеспечивают доступ только для предназначенных получателей
- Компрометация сервера не раскрывает содержимое сообщений без знания пароля

---

## 🧪 Тестирование

### 🦀 Rust SDK тесты
```bash
cd Ren-SDK
cargo test
```

### 🚀 Бэкенд тесты
```bash
cd backend
cargo test
```

### 📱 Flutter тесты
```bash
cd apps/flutter
flutter test
```

### 🌐 Фронтенд тесты
```bash
cd frontend
npm run test
```

---

## 🤝 Вклад в проект

Мы приветствуем вклад от сообщества! Вот как вы можете помочь:

### 🐛 Сообщения об ошибках
- Используйте GitHub Issues
- Предоставьте детальное описание
- Добавьте шаги воспроизведения

### 💡 Фич-реквесты
- Опишите идею подробно
- Объясните пользу для пользователей
- Рассмотрите техническую реализацию

### 🔧 Pull Requests
1. Форкните репозиторий
2. Создайте ветку для вашей фичи
3. Напишите тесты
4. Убедитесь что все тесты проходят
5. Отправьте PR

### 📋 Код стайл
- **Rust**: `cargo fmt` + `cargo clippy`
- **Flutter**: `dart format`
- **TypeScript**: ESLint + Prettier

---

## 📄 Лицензия

Этот проект распространяется под **Apache License 2.0**. Подробности в файле [LICENSE](LICENSE).

---

## 📞 Контакты

- **Автор**: Taiidzy
- **Email**: taiidzy@yandex.ru
- **GitHub**: [@taiidzy](https://github.com/taiidzy)

---

<div align="center">

**⭐ Если проект вам нравится, поставьте звезду!**

Made with ❤️ and Rust

</div>
