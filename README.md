# 🕊️ Ren - Безопасный мессенджер с E2EE шифрованием

<div align="center">

![Ren Logo](https://img.shields.io/badge/Ren-Messenger-blue?style=for-the-badge&logo=messaging)
![License](https://img.shields.io/badge/license-Apache%202.0-green?style=for-the-badge)
![Rust](https://img.shields.io/badge/rust-1.75+-orange?style=for-the-badge&logo=rust)
![Flutter](https://img.shields.io/badge/flutter-3.8+-blue?style=for-the-badge&logo=flutter)
![React](https://img.shields.io/badge/react-19+-cyan?style=for-the-badge&logo=react)

**Современный кроссплатформенный мессенджер с сквозным шифрованием (End-to-End Encryption)**

[🚀 Начать](#-быстрый-запуск) • [📖 Документация](#-документация) • [🔧 API](#-api-спецификация) • [🛡️ Безопасность](#-безопасность)

</div>

---

## ✨ Особенности

### 🔒 **Безопасность на первом месте**
- **Сквозное шифрование (E2EE)** - все сообщения шифруются на устройстве отправителя и расшифровываются только на устройстве получателя
- **Асимметричная криптография** - X25519 Elliptic Curve Diffie-Hellman для обмена ключами
- **Система конвертов** - каждое сообщение шифруется отдельным ключом для каждого получателя
- **ChaCha20-Poly1305 AEAD** для симметричного шифрования сообщений и файлов
- **Argon2id** для хеширования паролей И деривации ключей восстановления (memory-hard KDF)
- **PBKDF2-HMAC-SHA256** для деривации мастер-ключа из пароля (100,000 итераций)
- **Двойное шифрование приватных ключей**:
  - `pkebymk` - приватный ключ, зашифрованный мастер-ключом из пароля
  - `pkebyrk` - приватный ключ, зашифрованный ключом восстановления (для восстановления доступа)
- **HKDF-SHA256** для деривации ключей обёртки из ECDH shared secret
- **Zero-Knowledge архитектура** - сервер не имеет доступа к расшифрованным сообщениям
- **Защита от удаления чужих сообщений** - только автор или admin/owner могут удалять сообщения (P0-1)
- **Anti-replay защита** - идемпотентность сообщений через client_message_id (P1-6)
- **128-битная энтропия восстановления** - BIP39-style мнемонические фразы вместо 6-символьных кодов (P0-3)

### 🌐 **Кроссплатформенность**
- **Flutter** - мобильные приложения (iOS & Android) с нативной интеграцией Rust SDK через FFI
- **React + TypeScript** - современное веб-приложение с WASM модулем
- **iOS Native** - нативное SwiftUI приложение (в разработке)
- **Rust SDK** - единое ядро шифрования для всех платформ
- **WASM** поддержка для браузера

### 💬 **Современный функционал**
- **Приватные и групповые чаты** - автоматическое создание приватных чатов по каноническим парам пользователей
- **Обмен файлами с E2EE шифрованием** - до 50MB, потоковая загрузка, поддержка chunked файлов
- **Обмен изображениями и медиа-контентом** - с предпросмотром и метаданными
- **Индикаторы набора текста** - real-time typing indicators через WebSocket
- **Онлайн-статус пользователей** - presence система с подпиской на контакты
- **WebSocket соединение** - автоматическое переподключение с экспоненциальной задержкой
- **Управление аватарами** - загрузка и кэширование изображений
- **Редактирование и удаление сообщений** - с отметкой времени редактирования
- **Пересылка сообщений** - между любыми чатами с сохранением метаданных источника
- **Ответы на сообщения** - reply-to функциональность с ссылками на исходное сообщение
- **Избранные чаты** - система закладок для быстрого доступа
- **Локальные уведомления** - push-уведомления в Flutter приложении
- **Темная и светлая темы** - настраиваемые цветовые схемы
- **Кэширование файлов** - ETag поддержка для оптимизации загрузки медиа

### ⚡ **Производительность**
- **Axum 0.7** - высокопроизводительный асинхронный веб-сервер на Rust
- **PostgreSQL** - надёжная реляционная база данных
- **Tokio** - асинхронный runtime для Rust
- **DashMap** - быстрые concurrent структуры данных для WebSocket хаба
- **Оптимизированный Rust SDK** с LTO (Link Time Optimization)
- **SQLx** - типобезопасные SQL запросы на этапе компиляции

---

## 📌 Актуальный статус (2026-02)

- Mobile-first фокус: активная разработка в `apps/flutter`; `apps/iOS` и `frontend` не являются основным production-контуром.
- Transport hardening:
  - WebSocket auth без query token (только header-based flow).
  - На backend включена санитизация чувствительных query-параметров в логах.
- SDK attestation:
  - клиент отправляет `X-SDK-Fingerprint` в HTTP/WS;
  - backend поддерживает allowlist через `SDK_FINGERPRINT_ALLOWLIST` и привязку fingerprint к `auth_sessions`.
- Privacy controls:
  - Android anti-screenshot (`FLAG_SECURE`) и iOS privacy overlay / anti-capture управляются runtime-флагами (`--dart-define`), default OFF.
- Performance hardening для слабых устройств:
  - serial media pipeline, retry upload, isolate encryption, path-first media flow, state machine pending attachments.
- Supply-chain / verification:
  - для Ren-SDK добавлены новые macOS/Windows build-скрипты, формирующие verification bundle (`SHA256SUMS.txt`, `SDK_FINGERPRINT_ALLOWLIST.env`) и синхронизацию в backend.

---

## 🏗️ Архитектура

```
Ren/
├── Ren-SDK/          # 🦀 Rust ядро шифрования (FFI + WASM)
│   ├── src/
│   │   ├── crypto.rs      # Криптографические операции
│   │   ├── ffi.rs         # C ABI биндинги (iOS, Android, Flutter)
│   │   ├── wasm.rs        # WebAssembly биндинги (React)
│   │   └── lib.rs         # Главная библиотека
│   └── Cargo.toml
│
├── backend/          # 🚀 Axum сервер + PostgreSQL
│   ├── src/
│   │   ├── main.rs        # Точка входа, настройка сервера
│   │   ├── route/         # API маршруты
│   │   │   ├── auth.rs    # Регистрация, вход
│   │   │   ├── users.rs   # Профиль, поиск пользователей
│   │   │   ├── chats.rs   # Чаты и сообщения
│   │   │   ├── media.rs   # Загрузка/скачивание файлов
│   │   │   └── ws.rs      # WebSocket обработчик
│   │   ├── models/        # Модели данных
│   │   └── middleware/  # JWT аутентификация, логирование
│   ├── migrations/        # SQL миграции базы данных
│   └── Cargo.toml
│
├── frontend/         # 🌐 React веб-приложение
│   ├── src/
│   │   ├── Pages/         # Страницы (Login, Register, Home)
│   │   ├── components/   # UI компоненты (Radix UI)
│   │   ├── services/      # API клиенты
│   │   ├── context/       # React Context для состояния
│   │   └── lib/           # WASM SDK интеграция
│   ├── package.json
│   └── vite.config.ts
│
└── apps/
    ├── flutter/      # 📱 Flutter мобильное приложение
    │   ├── lib/
    │   │   ├── features/      # Модули (auth, chats, profile)
    │   │   ├── core/          # SDK интеграция, WebSocket клиент
    │   │   └── main.dart
    │   └── pubspec.yaml
    └── ios/          # 🍎 iOS нативное приложение (SwiftUI)
        ├── Ren/
        │   └── RenApp.swift
        └── Ren.xcodeproj
```

### 📦 Технологический стек

| Компонент | Технология | Версия | Описание |
|-----------|------------|--------|----------|
| **SDK** | Rust | 2021 edition | Кроссплатформенное ядро E2EE |
| **Бэкенд** | Axum | 0.7 | Асинхронный HTTP/WebSocket сервер |
| **Бэкенд** | Tokio | 1.x | Асинхронный runtime |
| **БД** | PostgreSQL | 14+ | Реляционная база данных |
| **БД миграции** | SQLx | 0.7 | Типобезопасные миграции |
| **Мобильные** | Flutter | 3.8+ | iOS & Android приложения |
| **Веб** | React | 19+ | UI библиотека |
| **Веб** | TypeScript | 5.9+ | Типизированный JavaScript |
| **Веб** | Vite | 7.2+ | Сборщик и dev-сервер |
| **Веб** | Tailwind CSS | 4.1+ | Utility-first CSS |
| **Веб UI** | Radix UI | Latest | Доступные компоненты |
| **Шифрование** | X25519-dalek | 2.0 | ECDH для обмена ключами |
| **Шифрование** | ChaCha20-Poly1305 | 0.10 | AEAD шифрование |
| **Хеширование** | Argon2 | 0.5 | Хеширование паролей |
| **JWT** | jsonwebtoken | 9.x | Аутентификация |

---

## 🚀 Быстрый запуск

### 📋 Требования

- **Rust** 1.75+ ([установка](https://rustup.rs/))
- **Flutter** 3.8+ ([установка](https://flutter.dev/docs/get-started/install))
- **PostgreSQL** 14+ ([установка](https://www.postgresql.org/download/))
- **Node.js** 18+ (если нужен `frontend/`)
- **Xcode CLT** (для iOS/macOS сборки на macOS)
- **Android SDK + NDK** (для Android)
- **cargo-ndk** (`cargo install cargo-ndk`)
- **cbindgen** (`cargo install cbindgen`)

### 🛠️ Рекомендуемая последовательность сборки

#### 1. Клонирование

```bash
git clone https://github.com/taiidzy/ren.git
cd ren
```

#### 2. PostgreSQL

```bash
psql -U postgres
CREATE DATABASE ren_messenger;
\q
```

#### 3. Backend

Создайте `backend/.env`:

```env
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_HOST=127.0.0.1
POSTGRES_PORT=5432
POSTGRES_DB=ren_messenger
JWT_SECRET=change-me-use-32-bytes-min

# Опционально: SDK attestation
SDK_FINGERPRINT_ALLOWLIST=

# Опционально: CORS allowlist
CORS_ALLOW_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,https://messanger-ren.ru,https://www.messanger-ren.ru
```

Запуск backend:

```bash
cd backend
cargo run
```

При старте автоматически выполняются миграции из `backend/migrations`.
Сервер по умолчанию слушает `http://0.0.0.0:8081`.

#### 4. Ren-SDK (рекомендуемый способ)

Новые скрипты сборки и доставки артефактов:

- macOS: `Ren-SDK/build.sdk.sh`
- Windows: `Ren-SDK/build.sdk.ps1`

Они выполняют единый pipeline:
- build SDK под релевантные платформы;
- copy в `apps/flutter` (Android `jniLibs`, iOS `RenSDK.xcframework` на macOS);
- формирование verification bundle с `SHA256SUMS.txt` и `SDK_FINGERPRINT_ALLOWLIST.env`;
- copy в `backend/sdk-verification/current`;
- опционально upload через `scp`.

macOS:

```bash
cd Ren-SDK
chmod +x build.sdk.sh
./build.sdk.sh
```

Windows (PowerShell):

```powershell
cd Ren-SDK
.\build.sdk.ps1
```

Флаги (одинаковое поведение):
- `--android-only`
- `--no-upload`
- `--no-sync-flutter`

Env переменные:
- `SDK_VERIFY_LOCAL_DIR` - локальный путь для verification bundle.
- `SDK_VERIFY_SCP_TARGET` - remote target для `scp` upload (`user@host:/path`).

Legacy-скрипт `Ren-SDK/build.sh` оставлен для обратной совместимости.

#### 5. Flutter app

```bash
cd apps/flutter
flutter pub get
flutter run
```

Privacy toggles (по умолчанию всё выключено):

```bash
flutter run \
  --dart-define=REN_ANDROID_FLAG_SECURE=false \
  --dart-define=REN_IOS_PRIVACY_OVERLAY=false \
  --dart-define=REN_IOS_ANTI_CAPTURE=false
```

Пример включения privacy-фич в release:

```bash
flutter build apk --release \
  --dart-define=REN_ANDROID_FLAG_SECURE=true
```

### 🧰 Скрипты запуска и сборки (рекомендуется)

В репозитории есть набор скриптов в `./scripts/`, которые:
- не зависят от того, из какой директории вы их запускаете (автоматически находят корень репо),
- умеют опционально собирать `Ren-SDK` перед запуском/сборкой,
- дают единый интерфейс для `ios/macos/windows/linux`.

Важно:
- **iOS и macOS можно запускать/собирать только на macOS**.
- **Windows и Linux обычно требуют соответствующую ОС** (на macOS скрипт предупредит, но Flutter чаще всего не соберёт/не запустит).

#### `./scripts/run.sh` — запуск приложения

Синтаксис:
```bash
./scripts/run.sh <ios|macos|windows|linux> [options] [-- <extra flutter args>]
```

Параметры:
- `--sdk`
  - Собрать `Ren-SDK` перед запуском.
  - Вызовет `./Ren-SDK/build.sh <platform>`.
  - Для `ios` дополнительно скопирует `Ren-SDK/target/RenSDK.xcframework` в `apps/flutter/ios/RenSDK.xcframework`.
- `--debug`
  - Запуск в debug режиме.
  - **Режим по умолчанию**.
- `--release`
  - Запуск в release режиме (`flutter run --release`).
- `--device <id>`
  - Пробросит `-d <id>` в `flutter run`.
  - Полезно, если нужно выбрать конкретный симулятор/устройство.
  - Если не указано:
    - для `macos/windows/linux` будет использовано `-d <platform>`,
    - для `ios` `-d` не задаётся (Flutter сам предложит/выберет устройство).
- `-- <extra flutter args>`
  - Всё, что идёт после `--`, будет передано в `flutter run` как есть.

Примеры:
```bash
./scripts/run.sh ios --sdk
./scripts/run.sh ios --device "00008110-0012345678901234"
./scripts/run.sh macos
./scripts/run.sh linux --release
./scripts/run.sh windows -- --verbose
```

#### `./scripts/build.sh` — сборка приложения

Синтаксис:
```bash
./scripts/build.sh <ios|macos|windows|linux> [options] [-- <extra flutter args>]
```

Параметры:
- `--sdk`
  - Собрать `Ren-SDK` перед сборкой.
  - Для `ios` дополнительно скопирует `Ren-SDK/target/RenSDK.xcframework` в `apps/flutter/ios/RenSDK.xcframework`.
- `--release`
  - Release сборка.
  - **Режим по умолчанию**.
- `--debug`
  - Debug сборка.
- `--no-codesign` (только iOS)
  - Добавит `--no-codesign` к `flutter build ios`.
  - Удобно для сборки без настроенных signing-профилей.
- `--ipa` (только iOS)
  - Вместо `flutter build ios` вызовет `flutter build ipa`.
  - Артефакт будет в `apps/flutter/build/ios/ipa`.
- `--output <dir>`
  - Скопирует получившийся артефакт(ы) в указанную директорию.
  - Пример: `--output dist/`.
- `-- <extra flutter args>`
  - Всё, что идёт после `--`, будет передано в `flutter build ...` как есть.

Типовые команды:
```bash
./scripts/build.sh ios --sdk --no-codesign
./scripts/build.sh ios --ipa
./scripts/build.sh macos --release --output dist/
./scripts/build.sh linux --debug
./scripts/build.sh windows --release
```

Пути артефактов (по умолчанию, без `--output`):
- **iOS**:
  - `flutter build ios`: `apps/flutter/build/ios/iphoneos`
  - `flutter build ipa`: `apps/flutter/build/ios/ipa`
- **macOS**: `apps/flutter/build/macos/Build/Products/Release` или `.../Debug`
- **Windows**: `apps/flutter/build/windows/x64/runner/Release` или `.../Debug`
- **Linux**: `apps/flutter/build/linux/x64/release/bundle` или `.../debug/bundle`

#### Алиасы

Для удобства есть короткие алиасы, которые просто прокидывают аргументы в основные скрипты:
- Запуск:
  - `./scripts/run-ios.sh ...`
  - `./scripts/run-macos.sh ...`
  - `./scripts/run-windows.sh ...`
  - `./scripts/run-linux.sh ...`
- Сборка:
  - `./scripts/build-ios.sh ...`
  - `./scripts/build-macos.sh ...`
  - `./scripts/build-windows.sh ...`
  - `./scripts/build-linux.sh ...`

5. **Запуск веб-приложения**
```bash
cd frontend

# Установите зависимости
npm install

# Запустите dev-сервер
npm run dev

# Или соберите для продакшена
npm run build
npm run preview
```

Веб-приложение будет доступно на `http://localhost:5173` (или другом порту, указанном Vite)

**Настройка API URL:**
Отредактируйте `frontend/src/config/env.ts` или создайте `.env` файл:

```bash
# .env
VITE_API_URL=http://localhost:8081
VITE_WS_URL=ws://localhost:8081
```

Для продакшена используйте переменные окружения при сборке:
```bash
VITE_API_URL=https://api.ren-messenger.com npm run build
```

---

## 📖 Документация

### 📚 Дополнительная документация

- **Backend API**: Подробная спецификация API в [`backend/specification.md`](backend/specification.md)
- **Frontend API**: Спецификация для веб-приложения в [`frontend/specification.md`](frontend/specification.md)
- **Ren-SDK**: Документация SDK в [`Ren-SDK/README.md`](Ren-SDK/README.md)
- **Flutter App**: Документация мобильного приложения в [`apps/flutter/README.md`](apps/flutter/README.md)

### 🔐 Архитектура шифрования

Ren использует асимметричную криптографию с системой конвертов (envelope encryption). Каждое сообщение шифруется уникальным симметричным ключом, который затем оборачивается (wraps) для каждого получателя отдельно.

#### 1. Регистрация пользователя

1. Клиент генерирует пару ключей X25519 (`generate_key_pair()`)
2. Публичный ключ (`pubk`) отправляется на сервер в открытом виде
3. Генерируется мастер-ключ из пароля через PBKDF2-HMAC-SHA256 (100k итераций) с солью
4. Генерируется ключ восстановления (recovery key) - случайная строка
5. Приватный ключ шифруется двумя способами:
   - `pkebymk` - зашифрован мастер-ключом (для обычного доступа)
   - `pkebyrk` - зашифрован ключом восстановления (для восстановления доступа)
6. Все зашифрованные данные и соль сохраняются на сервере
7. Пароль хешируется через Argon2id и также сохраняется

#### 2. Отправка сообщения

1. Отправитель запрашивает публичные ключи всех получателей
2. Генерируется случайный симметричный ключ для сообщения (`generate_nonce()`)
3. Сообщение шифруется симметричным ключом (ChaCha20-Poly1305)
4. Для каждого получателя создаётся конверт:
   - Симметричный ключ шифруется публичным ключом получателя (`wrap_symmetric_key()`)
   - Генерируется эфемерная пара ключей для ECDH
   - Результат упаковывается в конверт с nonce
5. Зашифрованное сообщение и конверты отправляются на сервер

#### 3. Получение сообщения

1. Получатель получает сообщение и свой конверт через WebSocket или REST API
2. Расшифровывает приватный ключ мастер-ключом (из пароля через PBKDF2)
3. Распаковывает симметричный ключ из конверта:
   - Использует свой приватный ключ и эфемерный публичный ключ из конверта
   - Вычисляет shared secret через ECDH
   - Деривирует ключ обёртки через HKDF-SHA256 с контекстом "ren-sdk-wrap"
   - Расшифровывает обёрнутый ключ через ChaCha20-Poly1305
4. Расшифровывает сообщение симметричным ключом (`decrypt_message()`)

#### 4. Файлы

Файлы шифруются аналогично сообщениям:
- Файл может быть разбит на чанки для больших размеров (>50MB не поддерживается)
- Каждый чанк шифруется отдельным ключом с уникальным nonce
- Метаданные файла (имя, тип, размер, дата создания) сохраняются в JSONB поле `metadata`
- Зашифрованный файл может быть встроен в `metadata.enc_file` или загружен отдельно через `/media/{id}`
- Файлы хранятся на диске в `uploads/media/` с UUID для предотвращения коллизий имен

### 🔄 Поток сообщений

```
┌─────────────┐                    ┌─────────────┐                    ┌─────────────┐
│ Отправитель │                    │   Сервер    │                    │ Получатель  │
└──────┬──────┘                    └──────┬──────┘                    └──────┬──────┘
       │                                   │                                   │
       │── 1. Запрос PK всех получателей ─>│                                   │
       │<── 2. Публичные ключи ───────────│                                   │
       │                                   │                                   │
       │── 3. Генерация msg_key ───────────│                                   │
       │── 4. Шифрование сообщения ───────│                                   │
       │── 5. Создание конвертов для ──────│                                   │
       │    каждого получателя             │                                   │
       │── 6. WebSocket: send_message ────>│                                   │
       │                                   │── 7. Сохранение в БД ─────────────>│
       │                                   │── 8. Проверка онлайн статуса      │
       │                                   │    и in_chat статуса              │
       │                                   │                                   │
       │                                   │── 9a. Если в чате ────────────────>│
       │                                   │    message_new событие            │
       │                                   │                                   │
       │                                   │── 9b. Если онлайн, но не в чате ──>│
       │                                   │    notification_new событие        │
       │                                   │                                   │
       │                                   │                                   │── 10. Расшифровка конверта
       │                                   │                                   │── 11. Расшифровка сообщения
       │                                   │                                   │── 12. Отображение в UI
```

### 📱 Клиентские приложения

#### Flutter

**Архитектура:**
- **Clean Architecture** с разделением на слои:
  - `data/` - API клиенты и репозитории (сетевые запросы, кэширование)
  - `domain/` - модели данных и бизнес-логика
  - `presentation/` - UI компоненты и страницы
- **Provider** для управления состоянием (DI через `MultiProvider`)
- **FFI интеграция** с Rust SDK через `dart:ffi`
- **WebSocket клиент** для real-time обновлений
- **Локальные уведомления** через `flutter_local_notifications`
- **Безопасное хранение** токенов и ключей через `flutter_secure_storage`

**Интеграция SDK:**
- Нативная библиотека загружается через `DynamicLibrary`:
  - **Android**: `libren_sdk.so` из `jniLibs/` (arm64-v8a, armeabi-v7a, x86, x86_64)
  - **iOS**: `RenSDK.xcframework` через `DynamicLibrary.process()` (статическая линковка)
  - **macOS**: поиск `libren_sdk.dylib` в нескольких локациях (Frameworks, Resources)
  - **Linux**: `libren_sdk.so` из системных путей
  - **Windows**: `ren_sdk.dll` из директории приложения
- Все функции SDK доступны через FFI биндинги в `core/sdk/ren_sdk.dart`
- Автоматическое управление памятью:
  - Строки C освобождаются через `ren_free_string()`
  - Байтовые массивы через `ren_free_bytes()`
  - Структуры через специализированные функции (`ren_free_key_pair`, `ren_free_encrypted_message` и т.д.)
- Singleton паттерн: `RenSdk.instance` для глобального доступа

**Безопасное хранение:**
- Использование `flutter_secure_storage` (Keychain на iOS, Keystore на Android)
- Хранимые ключи:
  - `token` - JWT токен аутентификации
  - `privateKey` - расшифрованный приватный ключ (base64)
  - `publicKey` - публичный ключ (base64)
  - `userId` - ID текущего пользователя
  - Настройки темы и фона
- Ключи никогда не логируются и хранятся в защищённом хранилище ОС

**WebSocket клиент:**
- Класс `RealtimeClient` с методами:
  - `connect()` - подключение с токеном из secure storage
  - `disconnect()` - отключение и очистка ресурсов
  - `init(contacts)` - инициализация с подпиской на контакты
  - `joinChat(chatId)` / `leaveChat(chatId)` - управление подписками на чаты
  - `sendMessage()` - отправка зашифрованных сообщений
  - `typing(chatId, isTyping)` - индикатор набора текста
  - `editMessage()`, `deleteMessage()`, `forwardMessage()` - операции с сообщениями
- События обрабатываются через `Stream<RealtimeEvent>` с broadcast
- Автоматическая обработка JSON парсинга и ошибок

**Работа с чатами:**
- **Кэширование файлов:**
  - Зашифрованные файлы кэшируются на диске (`getTemporaryDirectory()/ren_ciphertext_cache/`)
  - In-memory кэш для часто используемых файлов
  - Защита от дублирующих запросов через `_ciphertextInFlight`
- **Расшифровка сообщений:**
  - Автоматическое извлечение конверта для текущего пользователя
  - Распаковка симметричного ключа через `unwrapSymmetricKey()`
  - Расшифровка текста через `decryptMessage()`
  - Расшифровка вложений через `decryptFileBytes()`
- **Построение зашифрованных сообщений:**
  - Генерация уникального ключа для каждого сообщения
  - Создание конвертов для всех получателей
  - Self-check валидация: проверка возможности расшифровать собственный конверт
  - Поддержка текстовых сообщений, изображений и медиа-файлов
- **Обработка вложений:**
  - Загрузка зашифрованных файлов на сервер через `/media`
  - Сохранение метаданных в `metadata` поле сообщения
  - Поддержка `file_id` для ссылок на файлы на сервере

**Уведомления:**
- Локальные push-уведомления через `flutter_local_notifications`
- Канал уведомлений: `ren_messages` с высоким приоритетом
- Поддержка аватаров в уведомлениях (загрузка и кэширование)
- Обработка нажатий: переход к чату через callback `setOnOpenChat()`
- Настройка цвета уведомлений в зависимости от темы приложения
- Группировка уведомлений по чатам (стабильный ID = chatId)

**Ключ восстановления:**
- Генерация 6-символьного ключа из алфавита `A-Z0-9`
- Использование `Random.secure()` для криптографически стойкой генерации
- Ключ используется для шифрования `pkebyrk` (приватный ключ, зашифрованный ключом восстановления)

**UI компоненты:**
- **Glass morphism дизайн** - полупрозрачные поверхности с размытием
- **Адаптивные виджеты** - поддержка разных размеров экранов
- **Анимации** - плавные переходы и интерактивные элементы
- **Кастомные виджеты:**
  - `GlassSurface`, `GlassOverlay` - стеклянные поверхности
  - `MatteButton`, `MatteTextField` - стилизованные элементы ввода
  - `Avatar` - компонент аватара с кэшированием
  - `ContextMenu` - контекстные меню для сообщений
  - `AnimatedGradient` - анимированные градиенты для фона

**Процесс регистрации:**
- Двухшаговая форма регистрации:
  1. Ввод логина, пароля и подтверждения пароля
  2. Отображение сгенерированного ключа восстановления (6 символов)
- Генерация ключевой пары X25519 через SDK
- Создание мастер-ключа из пароля через PBKDF2
- Шифрование приватного ключа двумя способами:
  - `pkebymk` - мастер-ключом (для обычного доступа)
  - `pkebyrk` - ключом восстановления (для восстановления доступа)
- Валидация ключевых пар при входе (self-check через wrap/unwrap)

**Загрузка файлов:**
- Использование `multipart/form-data` для загрузки зашифрованных файлов
- Поддержка множественного выбора файлов через `file_picker`
- Поддержка изображений через `image_picker` (галерея и камера)
- Зашифрованные файлы загружаются на сервер через `/media` endpoint
- Метаданные файлов (имя, тип, размер, nonce) сохраняются в `metadata` сообщения
- Поддержка `file_id` для ссылок на файлы на сервере (вместо inline ciphertext)

**Обработка ошибок:**
- Кастомный класс `ApiException` для обработки ошибок API
- Автоматическая обработка DioException с извлечением сообщений об ошибках
- Валидация данных на клиенте перед отправкой
- Graceful degradation при ошибках расшифровки (показ "[encrypted]")
- Очистка secure storage при несовпадении ключей

**Зависимости:**
- `dio` - HTTP клиент для REST API
- `web_socket_channel` - WebSocket клиент
- `flutter_secure_storage` - безопасное хранение ключей
- `provider` - управление состоянием
- `image_picker` - выбор изображений из галереи/камеры
- `file_picker` - выбор файлов
- `flutter_local_notifications` - локальные уведомления
- `path_provider` - доступ к директориям приложения
- `mime` - определение MIME типов файлов
- `hugeicons` - иконки
- `logger` - логирование
- `ffi` - интеграция с нативными библиотеками

**Основные модули:**
- `features/auth` - регистрация и вход с генерацией ключей, валидация ключевых пар
- `features/chats` - список чатов, чат-комната с real-time обновлениями, отправка файлов
- `features/profile` - профиль пользователя, настройки темы и фона
- `features/splash` - экран загрузки с проверкой авторизации
- `core/sdk` - интеграция с Ren-SDK через FFI
- `core/realtime` - WebSocket клиент с обработкой событий
- `core/notifications` - локальные push-уведомления
- `core/secure` - безопасное хранение через secure storage
- `core/cryptography` - утилиты для криптографии (генерация ключа восстановления)
- `core/providers` - провайдеры для темы, фона, криптографии
- `shared/widgets` - переиспользуемые UI компоненты (glass surfaces, buttons, avatars)

#### React Web

**Архитектура:**
- React 19 с функциональными компонентами и хуками
- React Router v7 для навигации
- TanStack Query v5 для управления серверным состоянием с кэшированием
- Context API для глобального состояния (Auth, Realtime)
- WASM модуль Ren-SDK для шифрования (инициализация через `init()`)
- Radix UI для доступных компонентов
- Tailwind CSS v4 для стилизации
- Framer Motion для анимаций

**Интеграция SDK:**
- WASM модуль загружается асинхронно при старте приложения
- Все криптографические операции выполняются в браузере
- Автоматическая обработка ошибок и валидация данных

**WebSocket:**
- Автоматическое переподключение с экспоненциальной задержкой
- Поддержка подписки на контакты для presence
- Управление состоянием соединения (connecting, connected, disconnected, error)
- Обработка событий через React Context

**Основные страницы:**
- `/login` - страница входа с валидацией
- `/register` - страница регистрации с генерацией ключей
- `/` - главная страница со списком чатов
- `/chat/:chatId` - страница чата с real-time обновлениями

---

## 🔧 API Спецификация

### 🌐 Базовый URL

```
http://localhost:8081
```

### 🔑 Аутентификация

Все эндпоинты (кроме `/auth/*` и `/health`) требуют JWT токен в заголовке:

```
Authorization: Bearer <jwt_token>
```

### 📝 Основные эндпоинты

#### 👤 Аутентификация

```http
POST   /auth/register
Body: {
  "login": "string",
  "username": "string",
  "password": "string",
  "public_key": "string (base64)",
  "encrypted_private_key": "string (base64)",
  "salt": "string (base64)"
}
Response: {
  "user": UserResponse,
  "token": "string (JWT)"
}

POST   /auth/login
Body: {
  "login": "string",
  "password": "string"
}
Response: {
  "user": UserResponse,
  "token": "string (JWT)"
}
```

#### 👥 Пользователи

```http
GET    /users/me
Response: UserResponse

PATCH  /users/username
Body: { "username": "string" }
Response: UserResponse

PATCH  /users/avatar
Body: MultipartFormData (file)
Response: UserResponse

GET    /users/search?q=string&limit=10
Response: UserResponse[]

GET    /users/{id}/public-key
Response: { "public_key": "string (base64)" }

GET    /avatars/*path
Response: File (изображение аватара)

DELETE /users/me
Response: 204 No Content
```

#### 💬 Чаты

```http
POST   /chats
Body: {
  "kind": "private" | "group",
  "user_ids": number[],
  "title": "string" (для групп)
}
Response: Chat

GET    /chats
Query: ?limit=20&offset=0
Response: Chat[]

GET    /chats/{chat_id}/messages
Query: ?limit=50&before_id=123&after_id=456
Response: Message[]

POST   /chats/{id}/favorite
Response: 200 OK

DELETE /chats/{id}/favorite
Response: 200 OK

DELETE /chats/{id}
Response: 204 No Content
```

#### 📎 Медиа файлы

```http
POST   /media
Body: MultipartFormData (file, max 50MB)
Response: {
  "file_id": number,
  "filename": "string",
  "mimetype": "string",
  "size": number
}

GET    /media/{id}
Response: File (зашифрованный файл)
```

#### 🌐 WebSocket

**Подключение:**
```
GET /ws
Headers: Authorization: Bearer <jwt_token>
```

**События клиента → сервер:**

```json
// Инициализация (подписка на контакты)
{
  "type": "init",
  "contacts": [1, 2, 3]
}

// Присоединение к чату
{
  "type": "join_chat",
  "chat_id": 123
}

// Выход из чата
{
  "type": "leave_chat",
  "chat_id": 123
}

// Отправка сообщения
{
  "type": "send_message",
  "chat_id": 123,
  "message": "base64_encrypted_message",
  "message_type": "text" | "file" | "image",
  "envelopes": {
    "2": {
      "key": "base64_wrapped_key",
      "ephem_pub_key": "base64_ephemeral_key",
      "iv": "base64_iv"
    }
  },
  "metadata": [FileMetadata],
  "reply_to_message_id": 456
}

// Редактирование сообщения
{
  "type": "edit_message",
  "chat_id": 123,
  "message_id": 789,
  "message": "base64_encrypted_message",
  "envelopes": {...}
}

// Удаление сообщения
{
  "type": "delete_message",
  "chat_id": 123,
  "message_id": 789
}

// Пересылка сообщения
{
  "type": "forward_message",
  "from_chat_id": 123,
  "message_id": 789,
  "to_chat_id": 456,
  "message": "base64_encrypted_message",
  "envelopes": {...}
}

// Индикатор набора текста
{
  "type": "typing",
  "chat_id": 123,
  "is_typing": true
}
```

**События сервер → клиент:**

```json
// Успешная операция
{ "type": "ok" }

// Ошибка
{ "type": "error", "error": "error message" }

// Новое сообщение
{
  "type": "message_new",
  "chat_id": 123,
  "message": Message
}

// Обновлённое сообщение
{
  "type": "message_updated",
  "chat_id": 123,
  "message": Message
}

// Удалённое сообщение
{
  "type": "message_deleted",
  "chat_id": 123,
  "message_id": 789,
  "deleted_at": "2024-01-01T00:00:00Z",
  "deleted_by": 1
}

// Индикатор набора текста
{
  "type": "typing",
  "chat_id": 123,
  "user_id": 2,
  "is_typing": true
}

// Онлайн статус
{
  "type": "presence",
  "user_id": 2,
  "status": "online" | "offline"
}

// Уведомление о новом сообщении
{
  "type": "notification_new",
  "chat_id": 123,
  "message": Message
}
```

### 📊 Модели данных

#### UserResponse (публичный профиль)
```typescript
{
  id: number
  login: string
  username: string
  avatar: string | null  // путь к файлу аватара (например, "avatars/user_123.jpg")
}
```

#### UserAuthResponse (при входе/регистрации)
```typescript
{
  id: number
  login: string
  username: string
  avatar: string | null
  pkebymk: string  // зашифрованный приватный ключ (мастер-ключ)
  pkebyrk: string  // зашифрованный приватный ключ (ключ восстановления)
  salt: string     // соль для PBKDF2 (base64)
  pubk: string     // публичный ключ (base64)
}
```

#### Chat
```typescript
{
  id: number
  kind: "private" | "group"
  title: string | null  // только для групповых чатов
  created_at: string  // ISO8601
  updated_at: string  // ISO8601
  is_archived: boolean | null
  is_favorite?: boolean  // избранный чат
  peer_id?: number  // ID собеседника (для приватных чатов)
  peer_username?: string  // имя собеседника
  peer_avatar?: string | null  // аватар собеседника
}
```

**Примечание:** Для приватных чатов используется каноническая пара `user_a` и `user_b` (где `user_a < user_b`) для предотвращения дублирования чатов между одними и теми же пользователями.

#### Message
```typescript
{
  id: number
  chat_id: number
  sender_id: number
  message: string  // зашифрованное сообщение (base64)
  message_type: "text" | "file" | "image"
  created_at: string  // ISO8601
  edited_at: string | null  // ISO8601
  is_read: boolean
  has_files?: boolean
  metadata?: FileMetadata[]
  envelopes?: { [userId: string]: Envelope }
  reply_to_message_id?: number
  forwarded_from_message_id?: number
  forwarded_from_chat_id?: number
  forwarded_from_sender_id?: number
  status?: "pending" | "sent"  // для клиента
}
```

#### Envelope (конверт для E2EE)
```typescript
{
  key: string  // зашифрованный ключ (base64)
  ephem_pub_key: string  // эфемерный публичный ключ (base64)
  iv: string  // вектор инициализации (base64)
}
```

#### FileMetadata
```typescript
{
  file_id?: number  // ID файла в таблице media_files
  filename: string  // оригинальное имя файла
  mimetype: string  // MIME тип (например, "image/jpeg", "application/pdf")
  size: number  // размер файла в байтах
  enc_file?: string | null  // зашифрованный файл (base64) - опционально, может быть в отдельной записи
  nonce?: string | null  // nonce для файла (base64)
  file_creation_date?: string | null  // дата создания файла (ISO8601)
  // Для больших файлов, разбитых на чанки:
  nonces?: string[]  // массив nonce для каждого чанка
  chunk_size?: number  // размер чанка в байтах
  chunk_count?: number  // количество чанков
}
```

**Хранение файлов:**
- Файлы сохраняются в директории `uploads/media/` с именем `{user_id}_{uuid}`
- Метаданные хранятся в таблице `media_files` с ссылкой на чат и сообщение
- Доступ к файлам контролируется: владелец или участник чата
- Поддержка ETag для кэширования и оптимизации загрузки

---

### ⚠️ Known Security Limitations (2026-02-23)

Этот раздел документирует текущие ограничения безопасности и области для улучшения.

#### 🔴 Критические ограничения (в процессе исправления)

1. **Forward Secrecy / Post-Compromise Security** (P0-4)
   - **Статус**: Текущая E2EE схема использует статические ключи без Double Ratchet.
   - **Риск**: Компрометация долгосрочного ключа может раскрыть историю переписки.
   - **План**: Внедрение X3DH + Double Ratchet протокола.

#### 🟡 Устранённые ограничения (исправлено)

1. **TLS Hardening** (P2-11) ✅ **ИСПРАВЛЕНО**
   - **Было**: Отсутствие явного HTTPS redirect и HSTS.
   - **Стало**: 301 redirect HTTP→HTTPS, HSTS с preload, OCSP stapling.
   - **Конфигурация**: TLSv1.2/1.3, modern cipher suite, security headers.
   - **Файлы**: `nginx/nginx.conf`, `nginx/options-ssl-nginx.conf`

2. **Geo-Service Privacy** (P2-12) ✅ **ИСПРАВЛЕНО**
   - **Было**: Внешние geo-запросы по умолчанию отправляли IP клиентов.
   - **Стало**: Внешние geo-запросы отключены по умолчанию.
   - **Конфигурация**: `ENABLE_EXTERNAL_GEO=0` (default), city из trusted proxy headers.
   - **Файлы**: `backend/src/route/auth.rs`

3. **Rate Limiting** (P1-7) ✅ **ИСПРАВЛЕНО**
   - **Было**: Отсутствие защиты от brute-force и DoS атак.
   - **Стало**: Rate limiter с IP + account bucketing, exponential backoff.
   - **Конфигурация**: 5 попыток входа, lockout от 1 мин до 1 часа.
   - **Файлы**: `backend/src/middleware/rate_limit.rs`

2. **SDK Integrity Check** (P1-9) ✅ **ИСПРАВЛЕНО**
   - **Было**: Проверка целостности SDK не форсировалась при старте.
   - **Стало**: Явная проверка SHA256 hash на Android при инициализации.
   - **Поведение**: Приложение не запустится с модифицированным SDK.
   - **Файлы**: `apps/flutter/lib/core/sdk/ren_sdk.dart`

3. **Group/Channel E2EE** (P0-5) ✅ **ИСПРАВЛЕНО (частично)**
   - **Было**: Групповые чаты позиционировались как E2EE без предупреждения.
   - **Стало**: Явное предупреждение в UI о том, что групповые сообщения не защищены E2EE.
   - **Митигация**: Виджет `GroupE2EEWarning` отображается в заголовке групповых чатов.
   - **План**: Внедрение Sender Keys для полноценного группового E2EE.

2. **Public Key Authentication** (P0-2) ✅ **ИСПРАВЛЕНО**
   - **Было**: Отсутствие криптографической верификации публичных ключей.
   - **Стало**: Ed25519 identity keys для подписи X25519 публичных ключей.
   - **API**: `/users/:id/public-key` возвращает `{ public_key, signature, key_version, identity_key }`.
   - **Миграция**: БД миграция `20260223120000_p0_2_key_auth.sql`.

3. **Слабая recovery-схема** (P0-3) ✅ **ИСПРАВЛЕНО**
   - **Было**: 6-символьный recovery key (~31 бит энтропии).
   - **Стало**: 12-словная мнемоническая фраза BIP39 (~128 бит энтропии) + Argon2id KDF.
   - **Версия**: Ren-SDK v0.1.0+

2. **Удаление чужих сообщений** (P0-1) ✅ **ИСПРАВЛЕНО**
   - **Было**: Любой участник чата мог удалять любые сообщения.
   - **Стало**: Только автор сообщения или admin/owner могут удалять.
   - **Аудит**: Поля `deleted_at`, `deleted_by` для отслеживания удалений.

3. **Anti-replay защита** (P1-6) ✅ **ИСПРАВЛЕНО**
   - **Было**: Отсутствие защиты от повторной отправки сообщений.
   - **Стало**: `client_message_id` + unique constraint для идемпотентности.
   - **Версия**: Backend migration `20260223100000_p1_anti_replay.sql`

#### 🟢 Рекомендации по развёртыванию

- **JWT_SECRET**: Минимум 32 случайных байта
- **SDK_FINGERPRINT_ALLOWLIST**: Обязательно в production
- **HTTPS/WSS**: Всегда используйте TLS в production
- **HSTS**: Включите HTTP Strict Transport Security
- **Backup**: Регулярное резервное копирование БД с шифрованием
- **Мониторинг**: Логирование security-событий (неудачные логины, изменения ключей)

---

## 🛡️ Безопасность

### 🔒 Криптографические примитивы

- **Асимметричная криптография**: X25519 Elliptic Curve Diffie-Hellman (ECDH)
- **Симметричное шифрование**: ChaCha20-Poly1305 AEAD
- **Хеширование паролей**: Argon2id
- **Деривация ключей**: PBKDF2-HMAC-SHA256 (100,000 итераций)
- **Генерация случайных чисел**: `getrandom()` с системным источником
- **Система конвертов**: PKI-like подход для мультипользовательских чатов

### 🛡️ Меры защиты

- ✅ **Асимметричное шифрование** - только получатель может расшифровать свои конверты через свой приватный ключ
- ✅ **Двойное шифрование приватных ключей**:
  - Мастер-ключ из пароля (для обычного доступа)
  - Ключ восстановления (для восстановления доступа при потере пароля)
- ✅ **Защита от MITM** - проверка публичных ключей при регистрации и обмене
- ✅ **Мастер-ключ из пароля** - PBKDF2-HMAC-SHA256 с 100k итерациями и уникальной солью
- ✅ **Эфемерные ключи** - каждый конверт использует уникальную эфемерную пару ключей для ECDH
- ✅ **HKDF для деривации** - использование HKDF-SHA256 для безопасной деривации ключей из shared secret
- ✅ **Zero-Knowledge** - сервер не имеет доступа к расшифрованным сообщениям, только к зашифрованным данным
- ✅ **Отдельные конверты** - каждый участник получает свой зашифрованный ключ, даже в групповых чатах
- ✅ **JWT аутентификация + auth_sessions** - токены проверяются в связке с серверной сессией
- ✅ **CORS защита** - настраиваемая политика CORS через `CorsLayer`
- ✅ **Валидация входных данных** - проверка всех входных данных на сервере перед обработкой
- ✅ **Проверка прав доступа** - middleware для проверки членства в чатах перед операциями
- ✅ **Логирование без секретов** - middleware скрывает токены/секреты в query и payload
- ✅ **Header-only auth transport** - токены не принимаются из query string в защищённом API/WS flow
- ✅ **SDK attestation** - `X-SDK-Fingerprint` может быть обязательным и валидируется по allowlist + auth session
- ✅ **Client SDK integrity checks** - Android hash pinning и проверка фактически загруженного `libren_sdk.so`
- ✅ **FFI panic safety** - FFI entrypoints обёрнуты в panic-safe слой
- ✅ **Zeroization** - чувствительные ключевые буферы очищаются из памяти после использования

### ⚠️ Важные замечания

- Сервер хранит только зашифрованные данные и конверты - никогда не видит расшифрованный контент
- Приватные ключи хранятся на сервере в двойном зашифрованном виде:
  - `pkebymk` - зашифрован мастер-ключом (из пароля)
  - `pkebyrk` - зашифрован ключом восстановления (для восстановления доступа)
- Каждое сообщение использует уникальный симметричный ключ (32 байта, случайный)
- Каждый конверт использует уникальную эфемерную пару ключей для ECDH
- Конверты обеспечивают доступ только для предназначенных получателей
- Компрометация сервера не раскрывает содержимое сообщений без знания пароля или ключа восстановления
- **Важно**: В продакшене используйте:
  - Сильный `JWT_SECRET` (минимум 32 случайных байта)
  - Непустой `SDK_FINGERPRINT_ALLOWLIST` в backend
  - HTTPS для всех соединений
  - WSS для WebSocket
  - Безопасное хранение ключа восстановления (не на сервере!)
  - Регулярное резервное копирование базы данных

### 🔐 Рекомендации по безопасности

1. **Пароли**: Используйте сильные пароли (минимум 12 символов, смешанный регистр, цифры, символы)
2. **HTTPS**: Всегда используйте HTTPS в продакшене для защиты трафика
3. **JWT_SECRET**: Используйте криптографически стойкий случайный ключ (минимум 32 байта)
   ```bash
   # Генерация безопасного секрета
   openssl rand -base64 32
   ```
4. **База данных**: Ограничьте доступ к БД только для бэкенда, используйте SSL соединения
5. **Файлы**: Храните зашифрованные файлы в безопасном хранилище с ограниченным доступом
6. **Ключ восстановления**: Храните `pkebyrk` и ключ восстановления отдельно от сервера (например, у пользователя)
7. **Логирование**: Middleware автоматически скрывает пароли и токены в логах
8. **Ограничение размера**: Файлы ограничены 50MB для предотвращения DoS атак
9. **Валидация**: Все входные данные валидируются на сервере перед обработкой
10. **WebSocket**: Используйте WSS (WebSocket Secure) в продакшене

---

## 🧪 Тестирование

### 🦀 Rust SDK тесты

```bash
cd Ren-SDK
cargo test

# С WASM тестами
wasm-pack test --node

# С FFI тестами
cargo test --features ffi
```

### 🚀 Бэкенд тесты

```bash
cd backend
cargo test

# С подключением к БД (задайте POSTGRES_* переменные, как для `cargo run`)
POSTGRES_USER=postgres POSTGRES_PASSWORD=postgres POSTGRES_HOST=127.0.0.1 POSTGRES_PORT=5432 POSTGRES_DB=ren_messenger cargo test
```

### 📱 Flutter тесты

```bash
cd apps/flutter
flutter test

# Интеграционные тесты
flutter test integration_test/
```

### 🌐 Фронтенд тесты

```bash
cd frontend
npm run test

# С покрытием
npm run test:coverage
```

---

## 🗄️ База данных

### Структура таблиц

- **users** - пользователи
  - `id`, `login` (UNIQUE), `username` (UNIQUE), `avatar`
  - `password` (Argon2id hash)
  - `pkebymk`, `pkebyrk`, `pubk`, `salt` (E2EE ключи)
  
- **chats** - чаты
  - `id`, `kind` ('private' | 'group'), `title`, `created_at`, `updated_at`
  - `is_archived`, `user_a`, `user_b` (каноническая пара для приватных чатов)
  
- **chat_participants** - участники чатов
  - `chat_id`, `user_id` (PRIMARY KEY)
  - `joined_at`, `role` ('member' | 'admin'), `last_read_message_id`, `is_muted`
  
- **messages** - сообщения
  - `id`, `chat_id`, `sender_id`, `message` (зашифрованное)
  - `message_type` ('text' | 'file' | 'image'), `created_at`, `edited_at`
  - `envelopes` (JSONB - конверты для каждого участника)
  - `metadata` (JSONB - метаданные файлов)
  - `reply_to_message_id`, `forwarded_from_*` (для reply/forward)
  - `deleted_at`, `deleted_by`, `is_read`
  
- **media_files** - медиа файлы
  - `id`, `owner_id`, `chat_id`, `message_id`
  - `path` (относительный путь в uploads/), `filename`, `mimetype`, `size`, `created_at`
  
- **chat_favorites** - избранные чаты
  - `user_id`, `chat_id` (PRIMARY KEY), `created_at`

### Миграции

Миграции выполняются автоматически при запуске бэкенда через SQLx:

```bash
cd backend
cargo run  # Автоматически выполнит миграции из ./migrations
```

Ручной запуск миграций:

```bash
cd backend
sqlx migrate run
```

---

## 🚢 Развёртывание

### Docker (рекомендуется)

Создайте `docker-compose.yml` в корне проекта:

```yaml
version: '3.8'

services:
  postgres:
    image: postgres:14-alpine
    environment:
      POSTGRES_DB: ren_messenger
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-password}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-password}
      POSTGRES_HOST: postgres
      POSTGRES_PORT: 5432
      POSTGRES_DB: ren_messenger
      JWT_SECRET: ${JWT_SECRET:-change-this-in-production}
    ports:
      - "8081:8081"
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - ./uploads:/app/uploads  # Для хранения загруженных файлов

volumes:
  postgres_data:
```

**Создайте `.env` файл для Docker:**

```bash
POSTGRES_PASSWORD=your-secure-password
JWT_SECRET=your-super-secret-jwt-key-min-32-bytes
```

**Запуск:**

```bash
# Сборка и запуск
docker-compose up -d

# Просмотр логов
docker-compose logs -f backend

# Остановка
docker-compose down

# Остановка с удалением данных
docker-compose down -v
```

**Примечание:** Для продакшена создайте отдельный `Dockerfile` в директории `backend/` с multi-stage build для оптимизации размера образа.

### Продакшен

1. **Настройте переменные окружения**:
   ```bash
   export POSTGRES_USER=postgres
   export POSTGRES_PASSWORD=...
   export POSTGRES_HOST=127.0.0.1
   export POSTGRES_PORT=5432
   export POSTGRES_DB=ren_messenger
   export JWT_SECRET=strong-random-secret
   ```

2. **Соберите бэкенд**:
   ```bash
   cd backend
   cargo build --release
   ```

3. **Запустите сервер**:
   ```bash
   ./target/release/backend
   ```

4. **Настройте reverse proxy** (nginx):
   ```nginx
   server {
       listen 80;
       server_name api.ren-messenger.com;

       location / {
           proxy_pass http://localhost:8081;
           proxy_http_version 1.1;
           proxy_set_header Upgrade $http_upgrade;
           proxy_set_header Connection "upgrade";
       }
   }
   ```

---

## 🤝 Вклад в проект

Мы приветствуем вклад от сообщества! Вот как вы можете помочь:

### 🐛 Сообщения об ошибках

- Используйте [GitHub Issues](https://github.com/taiidzy/ren/issues)
- Предоставьте детальное описание проблемы
- Добавьте шаги воспроизведения
- Укажите версии используемого ПО

### 💡 Фич-реквесты

- Опишите идею подробно
- Объясните пользу для пользователей
- Рассмотрите техническую реализацию
- Создайте issue с меткой `enhancement`

### 🔧 Pull Requests

1. Форкните репозиторий
2. Создайте ветку для вашей фичи (`git checkout -b feature/amazing-feature`)
3. Напишите тесты для новой функциональности
4. Убедитесь что все тесты проходят
5. Следуйте код-стайлу проекта
6. Отправьте PR с подробным описанием изменений

### 📋 Код стайл

- **Rust**: `cargo fmt` + `cargo clippy`
- **Flutter**: `dart format` + `flutter analyze`
- **TypeScript**: ESLint + Prettier (`npm run lint`)

---

## 📄 Лицензия

Этот проект распространяется под **Apache License 2.0**. Подробности в файле [LICENSE](LICENSE).

**Важно:** Различные компоненты могут иметь разные лицензии:
- **Ren-SDK**: MIT License (см. `Ren-SDK/LICENSE`)
- **Основной проект**: Apache License 2.0

---

## 📞 Контакты

- **Автор**: Taiidzy
- **Email**: taiidzy@yandex.ru
- **GitHub**: [@taiidzy](https://github.com/taiidzy)

---

## 🗺️ Roadmap

- [ ] Голосовые и видеозвонки
- [ ] Групповые звонки
- [ ] Резервное копирование ключей
- [ ] Forward secrecy (долгосрочная цель)
- [ ] Post-quantum криптография
- [ ] Децентрализованная архитектура
- [ ] Боты и интеграции
- [ ] Стикеры и эмодзи
- [ ] Голосовые сообщения
- [ ] Статусы и истории

---

<div align="center">

**⭐ Если проект вам нравится, поставьте звезду!**

Made with ❤️ and Rust

</div>
