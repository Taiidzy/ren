---
title: Введение
description: Что такое Ren — безопасный мессенджер с E2EE шифрованием
sidebar_position: 1
---

# Введение

**Ren** — современный кроссплатформенный мессенджер со сквозным шифрованием (End-to-End Encryption, E2EE). Все сообщения шифруются на устройстве отправителя и расшифровываются только на устройстве получателя. Сервер не имеет доступа к содержимому переписки.

## Ключевые возможности

### 🔒 Безопасность

- **Сквозное шифрование (E2EE)** — X25519 Elliptic Curve Diffie-Hellman для обмена ключами, ChaCha20-Poly1305 AEAD для шифрования сообщений
- **Система конвертов** — каждое сообщение шифруется отдельным ключом для каждого получателя
- **Zero-Knowledge архитектура** — сервер хранит только зашифрованные данные
- **Защита от удаления чужих сообщений** — только автор или admin/owner могут удалять сообщения
- **Anti-replay защита** — идемпотентность сообщений через `client_message_id`
- **128-битная энтропия восстановления** — мнемонические фразы для восстановления доступа

### 💬 Функционал

- **Приватные чаты** — автоматическое создание по каноническим парам пользователей
- **Групповые чаты и каналы** — ролевая модель (member/admin/owner)
- **Обмен файлами с E2EE** — до 50MB, потоковая загрузка, поддержка chunked файлов
- **Индикаторы набора текста** — real-time typing indicators через WebSocket
- **Онлайн-статус** — presence система с подпиской на контакты
- **Редактирование и удаление сообщений** — с отметкой времени редактирования
- **Пересылка и ответы** — сохранение метаданных источника
- **Избранные чаты** — система закладок для быстрого доступа

### 🌐 Кроссплатформенность

- **Flutter** — мобильные приложения (iOS & Android) с нативной интеграцией Rust SDK через FFI
- **React + TypeScript** — веб-приложение с WASM модулем
- **iOS Native** — SwiftUI приложение (в разработке)
- **Rust SDK** — единое ядро шифрования для всех платформ

### ⚡ Производительность

- **Axum 0.7** — высокопроизводительный асинхронный веб-сервер на Rust
- **PostgreSQL** — надёжная реляционная база данных
- **Tokio** — асинхронный runtime
- **Оптимизированный Rust SDK** с LTO (Link Time Optimization)

## Архитектура проекта

```
ren/
├── Ren-SDK/          # Rust ядро шифрования (FFI + WASM)
├── backend/          # Axum сервер + PostgreSQL
├── frontend/         # React веб-приложение
├── apps/
│   ├── flutter/      # Flutter мобильное приложение
│   └── ios/          # iOS нативное приложение (SwiftUI)
├── nginx/            # Reverse proxy + TLS termination
└── docs/             # Документация
```

## Технологический стек

| Компонент | Технология | Версия |
|-----------|------------|--------|
| **SDK** | Rust | 2021 edition |
| **Бэкенд** | Axum | 0.7 |
| **Бэкенд** | Tokio | 1.x |
| **БД** | PostgreSQL | 14+ |
| **Мобильные** | Flutter | 3.8+ |
| **Веб** | React | 19+ |
| **Веб** | TypeScript | 5.9+ |
| **Шифрование** | X25519-dalek | 2.0 |
| **Шифрование** | ChaCha20-Poly1305 | 0.10 |
| **Хеширование** | Argon2 | 0.5 |

## Статус проекта

**Активная разработка** (2026-02)

- ✅ E2EE для приватных чатов
- ✅ Групповые чаты и каналы (non-E2EE, есть UI warning)
- ✅ Доставка/прочтение сообщений (галочки)
- ✅ Typing indicators, presence
- ✅ Зашифрованная загрузка файлов
- ✅ Восстановление через мнемонические фразы
- 🔄 iOS нативное приложение (в разработке)
- 🔄 Double Ratchet protocol (в планах)
- 🔄 Group E2EE Sender Keys (в планах)

## Известные ограничения безопасности

> **Важно:** Групповые и каналные сообщения в текущей версии **НЕ защищены E2EE**. Сервер имеет доступ к plaintext содержимому. В UI отображается предупреждение `GroupE2EEWarning`.

## Лицензия

Apache 2.0 — см. [LICENSE](https://github.com/taiidzy/ren/blob/main/LICENSE)

## Контакты

- **Email:** taiidzy@yandex.ru
- **Репозиторий:** https://github.com/taiidzy/ren
